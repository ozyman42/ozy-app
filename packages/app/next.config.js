const fs = require('fs');
const path = require('path');

const nextConfig = {};
/**
 *
 *reactStrictMode: true,
  swcMinify: true,
  images: {
    unoptimized: true
  },
  output: 'export'
  */

module.exports = nextConfig
